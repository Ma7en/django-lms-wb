
إليك كيفية القيام بذلك:

1. قم بإنشاء ملف ترحيل جديد باستخدام الأمر التالي:


python manage.py makemigrations --empty cores




2. افتح الملف الذي تم إنشاؤه وأضف الدالة التالية:



from django.db import migrations
from accounts.models import User
from cores.models import QuranPath

def add_default_data(apps, schema_editor):
    user = User.objects.get(id=1)  # افتراضيًا، يجب أن يكون هناك مستخدم برقم 1
    QuranPath.objects.create(
        user=user,
        title="مَسَارُ الحِفْظِ",
        description="خُطَّةٌ مُتكَامِلَةٌ لِخَتْمِ القُرْآنِ حِفْظًا وفَهْمًا.",
        name="save"
    )
    QuranPath.objects.create(
        user=user,
        title="مَسَارُ المُرَاجَعَةِ",
        description="بَرْنَامَجٌ مُكَثَّفٌ لِمَنْ خَتَمَ القُرْآنَ ويُرِيدُ تَثْبِيتَ حِفْظِهِ.",
        name="review"
    )
    QuranPath.objects.create(
        user=user,
        title="مَسَارُ الإِجَازَةِ",
        description="لِلْحُفَّاظِ المُتْقِنِينَ السَّاعِينَ لِنَيْلِ الإِجَازَةِ بِسَنَدٍ مُتَّصِلٍ.",
        name="vacation"
    )

class Migration(migrations.Migration):
    dependencies = [
        ('cores', '0008_chapterinquran_certificatequran_classroom_and_more'), # اسم الملف السابق
    ]
    operations = [
        migrations.RunPython(add_default_data),
    ]






3. قم بتطبيق الترحيل باستخدام الأمر التالي:

python manage.py migrate


















# Generated by Django 5.0.3 on 2025-07-04 17:55

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('cores', '0008_chapterinquran_certificatequran_classroom_and_more'),
    ]

    operations = [
    ]
